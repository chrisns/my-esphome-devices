name: "esphome"
on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]
jobs:
  compile:
    name: esphome
    uses: docker://ghcr.io/esphome/esphome:2023.6.4
    with:
      args: compile *.yaml
